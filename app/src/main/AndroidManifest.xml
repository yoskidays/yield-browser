<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application android:theme="@style/Theme.YieldBrowser" android:label="@string/app_name" android:usesCleartextTraffic="true" android:resizeableActivity="false">
        <activity android:name="com.yieldbrowser.app.MainActivity" android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize" android:exported="true" android:launchMode="singleTop" android:documentLaunchMode="never" android:excludeFromRecents="false"
            android:supportsPictureInPicture="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <receiver android:name="com.yieldbrowser.app.DownloadOpenReceiver" android:exported="false" />
    </application>
</manifest>
